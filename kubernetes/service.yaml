apiVersion: v1
kind: Service
metadata:
  name: enterprise-devops-app-service
spec:
  type: NodePort
  selector:
    app: enterprise-devops-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
