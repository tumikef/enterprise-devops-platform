apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise-devops.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise-devops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise-devops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
